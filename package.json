{
  "name": "solartrack",
  "version": "0.1.0",
  "description": "SolarTrack - Track and analyze your solar production with AI",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r type-check"
  },
  "devDependencies": {},
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
